# AGENTS.md
lumada-oss-lite ― 最上位ルール定義書（日本語）

本ドキュメントは、本リポジトリに関わる **すべてのAI（Codex等）と人間**が遵守すべき最上位ルールです。  
README や設計資料よりも **AGENTS.md を優先**します。

---

## 1. プロジェクトの目的
本プロジェクトは、Lumada的な思想（データ収集→統合→可視化→AI）を、OSSのみで再現する軽量PoCである。

狙いは「テンプレを当てること」ではない。  
**検討開始時点で、適切な方向性 × 有効なスコープサイズを固定**し、PoCの成功確率を最大化する。

---

## 2. Agentic AI としての成立条件（必須）
以下を **すべて満たす場合のみ** Agentic AI とみなす。

- 明確に役割分担された複数 Agent
- 中央制御を行う Orchestrator
- 失敗を返す Validator Agent
- 検証結果に基づく Retry（再試行）ループ
- Pydantic 等のスキーマによる決定論的な出力検証

---

## 3. Agent 構成（責務定義）
- ReaderAgent  
  異常イベント/ログを読み取り、登場人物・操作・条件・例外・前提を抽出する

- PlannerAgent  
  抽出結果を基に、保守/対応を実行可能なタスク単位に分解する

- ValidatorAgent  
  抜け漏れ・曖昧さ・矛盾・スキーマ違反を検出し、失敗判定（issues）を返す

- GeneratorAgent  
  検証済み情報のみを用いて、実行可能JSON（作業計画）を生成する

- Orchestrator  
  Agent実行順序・Retry制御・ログ要約を担当する

---

## 4. 日本語docstring 記載ルール【必須】
本リポジトリで作成・更新する **すべての Python ファイル**は、以下①〜⑤を **日本語で必ず記載**する。  
未記載の場合、実装は **未完成扱い**とする。

### 4.1 ① ファイルサマリー（必須）
各 `.py` の先頭に、モジュールドックストリング（ファイルサマリー）を記載する。
- このファイルの責務
- 主な入出力
- 重要な制約（例：最大Retry回数、ログ方針など）

### 4.2 ② クラス説明（必須）
- クラスの責務
- 主要メソッドの役割
- 前提・制約

### 4.3 ③ 関数説明（必須）
- 何をする関数か
- 引数の意味
- 戻り値の意味
- エラー条件（ある場合）

### 4.4 ④ 条件Note（必須）
if/else、retry、例外、環境変数分岐など **条件付きの挙動**がある場合、必ず `Note:` に条件を明記する。
- 「issues が存在する場合のみ retry する」
- 「retry は最大2回」
- 「generator は validation 通過後のみ実行する」等

### 4.5 ⑤ 主要変数の意味説明（必須）
関数内・クラス内で定義される主要変数について、日本語で意味説明を必ず記載する。
- 変数定義直前のコメント
- または docstring 内の `Variables:` セクション

---

## 5. コーディング規約（品質ゲート）
- Python は PEP8 準拠
- black / isort 互換（line length = 88）
- 依存の暗黙利用禁止（importの省略やグローバル依存を作らない）
- Agentの責務混在禁止（I/O層とロジック層を分離）
- 生のLLM応答・プロンプト全文をログに出さない（要約のみ）

---

## 6. Git運用（ブランチ戦略）
| ブランチ | 役割 |
| --- | --- |
| main | 常にデモ可能（compose起動・最低限動作が揃っている） |
| develop | 次リリース候補の統合先 |
| feature/* | 機能追加（短命） |
| fix/* | バグ修正（短命） |
| docs/* | ドキュメントのみ（短命） |

### PRルール
- main への直接pushは禁止（PR経由）
- develop → main のマージ時は、CHANGELOG更新と動作確認を必須
- PRテンプレのチェック項目（品質ゲート）を満たさない変更はマージしない

---

## 7. タグ運用（リリース）
- タグ形式：`vMAJOR.MINOR.PATCH`
- 原則：mainにマージされたタイミングでタグを打つ
- PoC期間は MINOR を上げる（機能が増えたら v0.1.0 → v0.2.0）
- バグ修正のみは PATCH（v0.2.0 → v0.2.1）
- CHANGELOG は `Unreleased` をリリース時に確定する

---

## 8. 禁止事項
- Agentic成立条件を満たさない「単発LLM」実装
- Validatorを通さない出力の採用
- 日本語docstring無しのPython実装
- secrets / APIキーのコミット
- `.venv` 等の環境依存ファイルのコミット
- PoCスコープを無断で拡張する変更（最初に決めた成功定義/KPIを壊す）

---

## 9. このルールの意図（Why）
本プロジェクトの目的は「AIにコードを書かせること」ではない。

- PoCの検討範囲を適切な方向性・有効な大きさで開始する
- AIの役割分担と検証を明示し、人がレビュー可能な形にする
- スコープ肥大を抑え、短期間で“意味のあるPoC”を成立させる

そのために、日本語docstring と validator/retry を設計要件として強制する。
